import React from 'react'

const ShowList = (props) => {
    // debugger;
    const hStyle = {
        color: 'black'
    }
    return (
        <div>
            <h1 style={hStyle}>Show</h1>
        </div>
    )
}

export default ShowList